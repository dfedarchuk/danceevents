{% for item in items -%}
    <div class="{{ class }}">
        {{ include('::blocks/themebox/' ~ grid ~ '.html.twig', {'item': item, 'module': 'article', 'options': {seeDetail: true, notShowCategories: true}}) }}
    </div>
{% endfor -%}
