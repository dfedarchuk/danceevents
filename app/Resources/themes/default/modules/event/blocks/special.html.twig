{% for item in items -%}
    <div class="{{ class }}">
        {{ include('::blocks/themebox/' ~ grid ~ '.html.twig', {'item': item, 'module': 'event', 'options': { smallDate:true } }) }}
    </div>
{% endfor -%}

